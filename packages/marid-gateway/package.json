{
  "$schema": "https://json.schemastore.org/package.json",
  "version": "1.17.13",
  "name": "@marid/gateway",
  "description": "Marid auth envelope — bearer tokens, scopes, rate limiting, audit — wrapping the OpenCode v1 server handler",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "bun test --timeout 30000",
    "typecheck": "tsgo --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}

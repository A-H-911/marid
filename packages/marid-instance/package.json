{
  "$schema": "https://json.schemastore.org/package.json",
  "version": "1.17.13",
  "name": "@marid/instance",
  "description": "Marid instance manager — isolated per-instance runtime trees (XDG-namespaced), port/PID lifecycle, and launchers over the shared server binary",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "bun test --timeout 60000",
    "typecheck": "tsgo --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}

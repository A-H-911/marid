---
status: Draft
version: v0.1
updated: 2026-07-07
owner: operator (STK-001)
---

# Risk Register (RISK-)

Scoring: Impact × Likelihood, 1–3 each (9 = act now). Owner is the operator (STK-001) unless delegated
to the execution agent (STK-002). MVP column: does the mitigation belong in the MVP?

| ID | Risk | I | L | Score | Mitigation | Trigger to act | MVP |
|---|---|---|---|---|---|---|---|
| RISK-001 | Upstream v1→v2 API/SDK migration breaks the Marid layer or strands it on a deprecated surface | 3 | 3 | 9 | Build on v1 behind marid-auth facade (C-4); standing sync-checklist item watching v2/sdk-next stability; migration decision pre-drafted | sdk-next README drops "experimental" / v1 routes deprecated | Yes |
| RISK-002 | Cross-instance state corruption (auth.json RMW lost-writes, LSP cache torn installs, shared log) | 3 | 2 | 6 | Directory namespacing per instance (no shared mutable files by construction); EXP-002 verifies | EXP-002 failure or any stray-write report | Yes |
| RISK-003 | Prompt injection via Telegram / repo content / tool output leads to unintended tool use | 3 | 2 | 6 | INV-001 policy: channel agent = restricted ruleset, default-deny sensitive tools, approval prompts; injection framing per R-10 (enforce at tool-authorization boundary) | Any policy bypass in testing | Yes |
| RISK-004 | Supply-chain compromise via runtime npm-install (providers/plugins) or upstream deps | 3 | 1 | 3 | **⚠ PH-4 audit (2026-07-07): the code-side controls are NOT yet built** — provider exact-version pinning, FR-064 dependency/secret/license CI scanning, and the empty-by-default plugin allowlist are all PH-5/WBS-5.1 (WBS-0.3 delivered only the CI skeleton; no scanning job exists, `.gitleaksignore` is orphaned). Currently mitigated only by the operator installing no plugins/providers beyond the profile. *Re-score I/L at operator review; controls land PH-5.* | Scanner alert; PH-5 start | Yes (CI part → PH-5) |
| RISK-005 | Solo-maintainer fork fatigue: syncs stop happening, fork drifts | 3 | 2 | 6 | Small enumerated patch surface (P-*); monthly cadence + CI conflict-detector doing the boring part; delta report keeps drift visible | Two skipped sync cycles | Yes (process) |
| RISK-006 | SSE firehose is live-only → gateway misses events on disconnect | 2 | 2 | 4 | **⚠ Corrected PH-5 (2026-07-07):** the claimed `?after=<seq>` replay does NOT exist (v1 firehose is live-only — contract v1.1 / AC-007) and the gateway `pump` has **no reconnect** (`gateway.ts:181-189`), so one SSE drop stalls the reply path — surfaced as an intermittent `marid-telegram (windows)` hang (~100s, no reply). Interim: bounded 3× retry-wrapper on the live E2E (`ci.yml`, operator-chosen); real fix = firehose reconnect + authoritative-state re-fetch (deferred-work #8). *Re-score I/L at operator review.* | EXP/integration test shows loss; windows hang observed 2026-07-07 | Yes |
| RISK-007 | Secrets leak into session history/logs and get relayed to channels | 3 | 1 | 3 | **⚠ Corrected by PH-4 audit (2026-07-07): the redaction layer is NOT implemented** (only the Telegram bot-token literal is masked in gateway logs; egress/log/export secret-value redaction is deferred to PH-5 — ADR-0007, AC-016 Partial). **Active** controls: audit separation (token *name*, no bodies); secrets only in env/hashed stores; and containment — the restricted channel agent cannot read `auth.json` (outside its workspace roots) to relay provider keys. Residual: in-workspace secrets in a transcript still egress un-redacted. *Re-score I/L at operator review.* | Redaction test failure; audit found redaction absent 2026-07-07 | Yes (redactor → PH-5) |
| RISK-008 | Windows parity gaps (no /proc status, .cmd launchers, path handling) | 2 | 2 | 4 | claudectl already solved launcher parity (R-11); 3-OS CI smoke mandatory (NFR-004) | CI matrix failure | Yes |
| RISK-009 | Private-release update friction (gh token needed for self-update) | 1 | 3 | 3 | Documented gh-authenticated install/update path; acceptable for single operator | Operator pain report | Yes (docs) |
| RISK-010 | The v2 queue/steering path is less finished than it looks (partially wired, R-03) | 3 | 2 | 6 | EXP-001 before relying on it; C-5 fallback (busy-lock) pre-selected | EXP-001 FAIL | Yes |
| RISK-011 | Upstream changes direction/license or goes private | 2 | 1 | 2 | MIT baseline snapshot preserved in fork history; adapter-layer fallback (C-1 E) documented | Upstream announcement | No (watch) |
| RISK-012 | Scope creep back toward multi-user/public deployment before MVP ships | 2 | 2 | 4 | Charter non-goals + scope-change rule (DEC required); auth designed with headroom (tokens w/ scopes) but not built out | Any FR added outside registers | Yes (process) |
| RISK-013 | Unofficial WhatsApp client gets the operator's number banned (ToS violation; heuristic ML enforcement; no sandbox) | 2 | 2 | 4 | Dedicated linked-device number (not primary), allowlist-only (single operator), throttled/human-paced messaging; treat account loss as an expected operational event; official Cloud API kept as rejected fallback (ADR-0010) | Ban observed; PH-7 start | No (PH-7) |
| RISK-014 | WhatsApp-library supply-chain compromise (e.g. Dec-2025 `lotusbail` malicious Baileys fork stole auth tokens/backdoored accounts) | 3 | 1 | 3 | Isolate the client behind a pinned WAHA container so Marid pulls no WhatsApp dep (primary), OR exact-pin + frozen lockfile + integrity + provenance check + install-script block if Baileys-direct; ties RISK-004 / NFR-005 (ADR-0010) | Advisory/CVE on the chosen lib; PH-7 start | No (PH-7) |
| RISK-015 | Cross-client mirroring correctness — cross-surface permission/concurrency, incl. **privilege escalation via mirroring** | 3 | 2 | 6 | **view-via-binding, act-via-ownership** (INV-001-safe by construction — `scope.ts:109` ownership gate); first-responder-wins/no-double-approve; EXP-008 proves it (ADR-0012) | any cross-surface approval bypass in testing | No (PH-6) |
| RISK-016 | Real-client Telegram E2E flakiness (test-DC `PHONE_CODE_INVALID`, stale GramJS, `/test`-bot setup, Web-Playwright selector/session fragility) | 2 | 3 | 6 | local-pre-PR + GitHub-on-demand (non-gating); the deterministic fake-server E2E stays the blocking gate; bounded retry; EXP-007/009 de-risk (ADR-0013) | EXP-007/009 fail | No (PH-6) |
| RISK-017 | Evolving marid-auth into the gateway breaks the **already-proven** INV-001 enforcement | 3 | 1 | 3 | keep TEST-AUTH/TEST-SEC/channel-binding green through the refactor; additive module, no change to the auth decision path (ADR-0011) | any TEST-AUTH/TEST-SEC regression | No (PH-6) |
| RISK-018 | Telegram test-DC instability/wipes; the bot must be registered on the test DC | 1 | 2 | 2 | throwaway/test-DC assets only, no secrets on test DC; the fake-server tier is unaffected | test-DC outage | No (PH-6) |
| RISK-019 | NFR-001: the mirroring server-side surface grows the upstream patch surface | 2 | 1 | 2 | **DOWNGRADED (verified `event-filter.ts`):** mirroring rides the existing firehose+filter — binding registry + binding-aware `isVisible` + channel-client are additive, no new bus, zero upstream edit; contain behind ONE `Server.extend` hook; residual = binding write-path iff OpenCode owns session metadata (one `P-*`); EXP-008 confirms | EXP-008 finds an unavoidable upstream edit | No (PH-6) |
| RISK-020 | Native mobile-app E2E flakiness (emulator/device + Telegram-app-version drift + impractical CI) | 1 | 3 | 3 | kept **manual/occasional, never a gate**; EXP-010 confirms feasibility else falls back to manual visual QA (ADR-0013) | EXP-010 too brittle | No (PH-6) |
| RISK-021 | WhatsApp text-reply permission is untrusted channel input (approval injection / ambiguity / replay) | 3 | 2 | 6 | token-bound (single-use, JID-bound, TTL) + strict exact-match parse (no NLP) + server-side scope re-check (INV-001); parser unit-tested (ADR-0015) | any approval parsed from ambiguous/free text | No (PH-7) |
| RISK-022 | Burner test accounts banned during the WhatsApp real-protocol probe (no sandbox exists) | 1 | 3 | 3 | probe is **manual/nightly, never a gate**; fake-WA is the deterministic gate; accept number loss (ADR-0014) | ban observed | No (PH-7) |
| RISK-023 | WhatsApp interactive UI (buttons/lists) unreliable/deprecated → permission UX must not depend on it | 2 | 2 | 4 | token-bound **text reply** is the primary + sufficient path; WAHA-Plus list is optional convenience only (ADR-0015) | list/button render failure | No (PH-7) |
| RISK-024 | **Gateway blast radius** — the gateway is a shared component all interfaces (TUI/Web/API/channels) depend on; a defect could affect everything, not just channels | 3 | 2 | 6 | **strict path isolation** (auth/scope/event-filter path untouched — TEST-AUTH/SEC green); binding-aware `isVisible` is a **strict superset** (no binding ⇒ plain-client path byte-for-byte unchanged); **graceful degradation** (mirroring/registry fault → today's non-mirrored behavior, never breaks auth/plain clients); new attach/binding endpoints **additive + OpenAPI + health + TEST-CONTRACT-pinned** (ADR-0011); EXP-008 tests degradation | any plain-client regression traced to the gateway module | No (PH-6) |

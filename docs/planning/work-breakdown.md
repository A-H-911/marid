---
status: Approved
version: 1.0.0
updated: 2026-07-12
owner: operator (STK-001)
---

# Work Breakdown (WBS-)

Phase → work-item decomposition. Every leaf has a Definition of Done and traces to FRs/NFRs/ADRs/KPIs/RISKs.
Phases and their exit milestones are in [roadmap](roadmap.md) and [milestones](milestones.md); execution
follows TDD per [test strategy](../validation/test-strategy.md). Status `✅ done` marks completed leaves.
**PH-5 complete (MS-006, 2026-07-09): all of WBS-5.1..5.5 done.**

## PH-0 Foundations — ✅ COMPLETE (MS-001 met, 2026-07-04)
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-0.1 | Create private repo from local clone; add `upstream` fetch-only remote; tag baseline | Repo exists; INV-003 respected; baseline tag pushed | Gate 11, ADR-0001 | ✅ done |
| WBS-0.2 | Import planning package + local artifacts via `feature/planning-package` PR | Gate-12 approval; nothing committed silently | INV-003 | ✅ done |
| WBS-0.3 | Branch protection + CI skeleton (lint, typecheck, unit, 3-OS smoke) | Required checks enforced on main/develop | FR-062/064 | ✅ done (+ CI flake class fixed, P-CI-4, PR #10) |
| WBS-0.4 | EXP-001 concurrency probe | PASS/FAIL report; DEC recorded if FAIL | HYP-001, RISK-010 | ✅ PASS — no marid concurrency layer needed |
| WBS-0.5 | EXP-002 isolation probe | Report; leaking paths enumerated if any | HYP-002, RISK-002 | ✅ PASS (audit) — live tree-diff deferred |
| WBS-0.6 | EXP-003 Telegram cadence probe | Report; cadence constants fixed | HYP-003 | ✅ PASS (live) — ≥2s cadence, 0×429 |
| WBS-0.7 | EXP-004 profile build probe | Report; P-1 seam question answered | HYP-004, DEC-001 | ✅ PASS (analysis) — P-1 dropped; live build deferred |

## PH-1 Marid layer — ✅ COMPLETE (MS-002 met, 2026-07-04, PR #13)
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-1.1 | `marid` distribution profile (workspace filter, build scripts, binary target) | Binary builds on 3 OSes; excluded pkgs absent | FR-060, ADR-0002 | ✅ done (additive `src/marid.ts` + `script/marid-build.ts`, P-ENTRY; 3-OS `marid-build` CI green; hygiene grep) |
| WBS-1.2 | marid-auth: bearer tokens + scopes (`marid token` CLI) | 401 without token; scopes enforced; unit+integration tests | FR-031 | ✅ done (`@marid/auth`; 401/403; client scope fork-aware + durable ownership DEC-011) |
| WBS-1.3 | marid-auth: rate limiting + audit JSONL + request-ID | 429 behavior; audit lines complete; redaction test | FR-030/032/033, FR-059 | ✅ done (token-bucket 10/s·30, SSE cap 4, 429+retry-after; audit +session; x-request-id echo) |
| WBS-1.4 | Contract tests pinning committed v1 routes/events | Suite fails on any breaking upstream change | FR-035, RISK-001 | ✅ done (TEST-CONTRACT: routes via `Server.openapi()` + events via `EventManifest` + live `/event` SSE) |
| WBS-1.5 | Branding pass P-2 (name/TUI title/user-agent) + config defaults P-3 (`lsp:false` etc.) | Patch-surface register updated; grep-based hygiene test (Shaheen pattern) | FR-065, NFR-001 | ✅ identity done (name + `serve`/`token`); cosmetic P-2 (README/TUI title/UA/logo) + config P-3 deferred to PH-5 (DEC-013) |

## PH-2 Instances — ✅ COMPLETE (MS-003 met, 2026-07-05, PR #17)
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-2.1 | marid-instance: add/list/path/start/stop/status/remove; env composition; launchers (3 OSes) | Commands work; `list --json`; 0700 dirs | FR-053, ADR-0006 | ✅ done (new `@marid/instance` pkg — `composeInstanceEnv` = EXP-002 env set; `MaridInstanceCommand` in `marid.ts`; `list --json`; 0700 trees; launcher via `process.execPath`) |
| WBS-2.2 | Port allocation + PID files + graceful shutdown | No orphan processes; port collisions impossible | FR-053 | ✅ done (race-free port via `--port 0` + logfile readiness; PID/port record; **idempotent start guard** — a second `start` returns the live record, never spawns a second server; platform-split tree-kill — `taskkill /T` on Windows, POSIX signals the process group *and* bare pid SIGTERM→SIGKILL; spiked on Windows) |
| WBS-2.3 | Multi-instance isolation test suite (from R-05 inventory + EXP-002) | KPI-003 green in CI | NFR-008 | ✅ done — MS-003 MET: fast unit tier (every R-05 row) + live 2-instance diff (`instance-isolation.test.ts`, `MARID_ISOLATION=1`) green in the 3-OS `marid-isolation` job (ubuntu 39s / macOS 53s / windows), on every PR-#17 run incl. the final all-green ×2 |

## PH-3 Cross-interface — ✅ COMPLETE (MS-004 met, 2026-07-05, PR #19; synced to main via #20 @862c7bd6fc)
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-3.1 | Instance-default launch mode: TUI attaches to instance server | §7 flow works locally | FR-038/042, ADR-0004 | ✅ done — `marid instance attach <name>` (resolves running instance URL + bearer token → the upstream TUI attach path; headers flow to HTTP **and** SSE via the SDK, zero upstream edit). TUI-as-client wire behavior proven by the §7 E2E; interactive-renderer driving is out of automated scope (no repo precedent, 3-OS flake) |
| WBS-3.2 | Reconnect/replay client behavior (SDK guidance + gateway lib) | No event loss across restart in test | FR-036/043, RISK-006 | ✅ done — kill+restart E2E: reconnecting client re-fetches authoritative history written before the restart (no state loss). Firehose is live-only (no `?after=` — contract v1.1 correction); recovery = authoritative-store re-fetch. Speculative gateway package deferred to PH-4 (its consumer, YAGNI); reconnect pattern documented as SDK guidance in the contract |
| WBS-3.3 | Concurrency semantics doc + tests (from EXP-001 result) | Documented queue/steer behavior; tests assert it | FR-040/041 | ✅ done — api-event-contract v1.1 *Concurrency* section (join/steer, BusyError, abort, ordering); concurrency E2E asserts it through the authenticated marid wrapper (two clients, one session, no corruption) |

## PH-4 Telegram — ✅ COMPLETE (MS-005 met, 2026-07-07, PR #23)
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-4.1 | marid-telegram: long polling, `update_id` dedup, allowlist | Unknown senders ignored; dedup test | FR-046/050/051 | ✅ done — long-poll ingress + operator-id allowlist + `update_id` dedup store; AC-010 proven live in the 3-OS TEST-TG job (stranger ignored + logged) |
| WBS-4.2 | Egress: HTML formatting, 4096-split, edit-coalesced streaming, typing action | Cadence within EXP-003 constants; 429 honored | FR-048 | ✅ done — HTML/4096-split edit-coalesced streaming at the EXP-003 cadence with 429 handling; AC-011 proven live in TEST-TG (progressive edits, complete) |
| WBS-4.3 | Permission prompts as inline keyboards → `/permission/:id/reply` | Approve/deny round trip < 5 s | FR-028, KPI-002 | ✅ done — race-safe exactly-once inline-keyboard permission flow → `permission.respond`; round trip proven via the faked-SDK integration test (event→keyboard→Deny→reject) |
| WBS-4.4 | Capability policy wiring (restricted agent + `channel:` token + caps) | Policy denial paths tested | FR-052, INV-001 | ✅ done — `@marid/auth` INV-001 by-construction backstop: channel route-allowlist (deny-by-default on owned-session sub-routes, cannot reach /shell or /command) + token-bound-agent body guard; `channel-binding` + `scope` tests |
| WBS-4.5 | Media within Bot-API caps | Send/receive tests | FR-049 | ✅ done — full media send + receive within Bot-API caps; media tests |

## PH-5 Release & sync
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-5.1 | Public GitHub Releases pipeline (binaries, sha256, minisign signatures; upstream publish channels stripped) | Signed/checksummed release from tag | FR-060/064, C-6 | ✅ done — release pipeline (PR #27): `marid-release.yml` + `marid-build.ts --release` (tar/zip + sha256) + minisign signing; trust anchor wired (`minisign.pub` committed, secret `MINISIGN_SECRET_KEY`). Verified end-to-end (run 28892667716 green; throwaway prerelease signed+checksummed then deleted). AC-013 Met; AC-014 **Partial** (install path + 3-OS smoke = WBS-5.2). |
| WBS-5.2 | Install/update path for public releases (anonymous download + verify; DEC-010) | Documented + smoke-tested on 3 OSes | ~~RISK-009~~ (dissolved, DEC-010) | ✅ done — public `v0.1.0` RC cut (#35→main, merge-commit `8bf4ab61e`); documented anonymous download→`minisign -Vm`→`sha256sum -c`→run path (README); 3-OS install-smoke on the published signed assets (Linux+Windows green; macOS asset-name typo fixed forward PR #38); `marid upgrade` footgun removed. AC-014 Met. |
| WBS-5.3 | Upstream sync workflow automation (weekly check, monthly merge PR, delta report) | One real sync cycle executed | FR-061, KPI-004 | ✅ done — sync automation (PR #28): `marid-sync-upstream.yml` (weekly conflict-check + monthly merge PR, delta + migration-review + dependency-diff) **and one real 91-commit cycle merged via merge-commit (PR #31, KPI-004)**; `upstream/dev` now an ancestor of develop. AC-015 Met. Codemode reconciled per ADR-0002 (`external` + hygiene allowlist). |
| WBS-5.4 | README + attribution/non-affiliation + logo (per branding brief) | README complete; SVG logo committed | FR-065 | ✅ done — Marid README (attribution + minisign verify + `docs/branding/` logo); flame `mark.svg` + `logo-{light,dark}.png` lockup (operator-designed via Claude Design: Pixelify-Sans "Marid", blue face + orange offset, yellow→orange→red flame); P-2 realized (TUI title + startup logo redrawn, UA dropped w/ rationale); P-3 realized (`lsp:false` distribution default, overridable). FR-065 full. |
| WBS-5.5 | Readiness: traceability check, KPI evidence, docs validation | KPI-005/006 green; readiness report accepted | INV-008 | ✅ done — G-IDS fixed (audit rows now reference criteria defs), FR-064 re-marked `partial` (§18 scans/SBOM deferred, ADR-0007), AC-014 text corrected to public/anonymous (DEC-010); `validate_package.py docs/` = **RESULT: OK**; readiness report authored. **Gate-14 ACCEPTED 2026-07-09.** |

## PH-6 Telegram-first: gateway + full experience + mirroring + tests — post-MVP (all-in-one; ADR-0009/0011/0012/0013 Proposed; not-started, operator-gated)
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-6.1 | **Marid Gateway** — evolve marid-auth into the gateway (marid-auth = a module; mandatory bearer + scope; device-token scope-binding concept) + extract a thin **`@marid/channel-client`** (SDK v2 + channel-token + event-pump + session-binding + streamer + reconnect); one `Server.extend`-style hook for new server-side surface | Gateway boots; channel-client wired; TEST-AUTH/TEST-SEC/channel-binding stay green (INV-001 unbroken) | ADR-0011, DEC-017, FR-031/032/033/035, NFR-001, RISK-017 | ✅ done (parts a/b — merged, PR #46) — split advisor-confirmed into **6.1a** (channel-client extraction) + **6.1b** (attach endpoint + `/event` routing + bound-session consume; AC-019/AC-024). **6.1a:** new additive `@marid/channel-client` (firehose subscribe/pump + cross-generation event interpretation + `parseAskEvent` + per-part streamer coordination) extracted from `marid-telegram/gateway.ts`; gateway consumes it via `createChannelClient`/`beginTurn`/`start()` and keeps only Telegram specifics (chat↔session binding, rendering sink, keyboards); `parseAskEvent` re-exported (public API unchanged); gateway.ts −87 lines. Behavior-preserving (subscription stays on `global.event`, no server/auth edit — RISK-017): **marid-auth 101 / marid-telegram 89 unchanged & green**; channel-client +10 tests; typecheck+lint clean; +1 Marid-owned CI step; **zero upstream edit, no P-\***. Reconnect deferred to WBS-6.5 (only the pump *structure* extracted). "Gateway boots" already holds — marid-auth wrapper is the ingress; the `Server.extend` hook is only needed once **6.1b** adds server surface. **6.1b (in-progress — plan in `~/.claude/plans/wbs-6.1b-slice-b-scope.md`; part 1 ✅ done on branch):** **part 1 DONE** — admin-gated `POST /marid/attach`/`detach` + `GET /marid/bindings` served in the marid-auth wrapper writing `BindingStore`, admin-scope only; `GET /doc` augmented with a hand-authored Marid OpenAPI fragment (strip `accept-encoding` then merge; no effect dep); marid-auth **101→109**, zero upstream edit. **Part 2 DONE** — fine-filter `/global/event` with binding-aware `isVisible = owns ∪ bound` (the linchpin): web (`app`) + TUI + channel all ride `/global/event`, so this ONE change closed the pre-existing **INV-001 boundary gap** (unfiltered for every non-admin token, not just Telegram) **and** delivers mirroring (advances AC-019 — mechanism live + operator-reachable; the real-account live bidirectional E2E flips it to Met at WBS-6.6). A wrapped-frame picker (`owningSessionGlobal`) reads BOTH the regular twin (`payload.properties.sessionID`) **and the durable `sync` twin** (`payload.syncEvent.aggregateID`, `ses`-prefixed): `event-v2-bridge` emits both with the SAME `data`, so filtering only the regular twin would have LEAKED the durable copy of a non-owned session — a real INV-001 leak the naive picker would have shipped (caught by grepping the sync publish path; **security correction to the slice-b plan**, `SyncEvent.*` is NOT a blanket-pass). `filterSseStream` now takes a pluggable extractor (`/event` byte-unchanged). **Part 3 DONE** — channel-client lazily creates a streamer for any untracked session (post-filter = operator-bound), not only on `beginTurn`; Telegram renders bound sessions into the single operator's `defaultChatId` (CLI derives it from a single-operator allowlist). Attach-triggered reconnect stays WBS-6.5 (hook noted). **OWED DONE** — live `/doc` merge assertion in the `marid-sync` job (real gzip→strip→merge; guards the silent-failure `content-encoding` path). marid-auth **109→119**, channel-client **10→11**, marid-telegram **89→90**, all green; typecheck+lint clean; **zero upstream edit, no `P-\*`**. **Merged to develop (PR #46).** |
| WBS-6.2 | **Full Telegram experience** (fix-in-place) — Markdown via `telegramify-markdown`; **files both ways** (inbound `resolveDownloadUrl`→`FilePartInput` + outbound send); **whitelisted slash commands**; inline keyboards; multi-part separation | Full round-trip incl. files+slash+inline-kbd; INV-002/004 respected | ADR-0009, DEC-014, FR-046/048/049, DEP-012 | ✅ done (impl) — 4 UX defects fixed in place: MarkdownV2 render (`telegramify-markdown`, split-then-render, 400→plain fallback); **inbound files land** (`resolveDownloadUrl`→`FilePartInput`, filename traversal guard, token URL never logged — INV-002/004); deny-by-default **slash whitelist** (`/new`,`/help`); **multi-part separation** (per-part streamers). Inline keyboards pre-existed (`permission.ts`). marid-telegram suite 68→**89 green**, typecheck clean; **EXP-005 PASS** confirms the fix-in-place approach. **Merged to develop (PR #48);** live 3-OS `marid-telegram` + real-client E2E = WBS-6.6. |
| WBS-6.3 | **Bidirectional mirroring** — session↔surface **binding registry** (explicit-attach: `/attach`); binding-aware **`isVisible`** filter (edit `event-filter.ts`); channel-client consumes bound sessions; SSE fan-out (`seq`→`id:`, refresh-on-gap) | An attached session mirrors both ways; unattached does not appear; additive (no upstream edit) | ADR-0012, DEC-018, FR-066/038/042, RISK-019 | ✅ done (mirroring mechanism) — durable `BindingStore` (`binding.json` 0600 sidecar; attach/detach/list, mirrors `ownership.ts`) + binding-aware **`isVisible`** = `owns ∪ bound` at the `/event` filter site (`middleware.ts`; **`event-filter.ts` UNTOUCHED** — the "(edit event-filter.ts)" DoD text is stale, `filterSseStream` already takes an arbitrary predicate, so the swap is at the call site — EXP-008 governs). **View-via-binding** on the firehose only; **act-via-ownership** (`authorize`/list routes stay on `owns` → a bound surface views but cannot approve/prompt a non-owned session, INV-001) + degrade-safe `.catch` (registry fault → owns-only, RISK-024). `binding.test` + `mirroring.test` (production successor to the EXP-008 spike): marid-auth **87→100 green**, typecheck+lint clean, zero upstream edit. **Carried to siblings (documented split, not silently dropped):** channel-client-consume → WBS-6.1 (`@marid/channel-client`); seq→id fan-out + attach-triggers-reconnect → WBS-6.5; operator/admin-gated `/attach` HTTP endpoint + cross-surface permission first-responder-wins → WBS-6.4. **Merged to develop (PR #44).** |
| WBS-6.4 | **Cross-surface permission + concurrency** — permission surfaces across bound surfaces (first-responder-wins, no double-approve); **view-via-binding, act-via-ownership**; join/steer spans channels | No privilege escalation via mirroring; cross-surface permission + concurrency tests green | ADR-0012, INV-001, FR-028/040/041, RISK-015 | ✅ done (verification — code-light by design; the DoD is "tests green"). **first-responder-wins / no-double-approve** proven at the server invariant: `Permission.reply` get-check-`pending.delete` has no `yield*` between get and delete → atomic on one instance; first reply consumes, second fails `NotFoundError` (`packages/opencode/test/permission/next.test.ts` new test + the pre-existing "reply - fails for unknown requestID"). **view-via-binding of the ask** — a `permission.asked` frame on a bound session mirrors to the attached surface (`mirroring.test.ts` new test). **act-via-ownership** — a bound-but-not-owner surface is DENIED replying/prompting (already proven in `mirroring.test.ts`, WBS-6.3). **Concurrency (FR-040/041)** — the one-Runner join/steer is **EXP-001 (PASS)**; a channel can only *act* on sessions it owns (act-via-ownership), so cross-surface acting is same-owner multi-connection, covered by EXP-001 — not rebuilt (advisor-confirmed). marid-auth **100→101 green**, opencode permission **79→80 green**, typecheck clean, zero upstream edit. **Moved out (AC-024 constraint):** the operator-reachable **admin-gated `/attach` HTTP endpoint** belongs in **WBS-6.1** (OpenAPI derives from the Effect HttpApi `OpenApi.fromApi(PublicApi)`, so a marid-auth-wrapper route gets no OpenAPI/health/contract coverage AC-024 requires) — bindings written in-test via `BindingStore` here. Reconciliation: a `channel:` token can't self-attach (INV-001 landmine), so ADR-0012's "operator explicitly attaches" is an **admin-surface** action (TUI/CLI/web), not a Telegram `/attach` by the channel token. **Merged to develop (PR #48).** |
| WBS-6.5 | **SSE reconnect + re-fetch recovery** (folds deferred #8) — reconnect the firehose, re-fetch authoritative state, resume; attach triggers reconnect (mid-stream binding) | Injected SSE drop auto-recovers; reconnect test | FR-036/043, RISK-006 | ✅ done (merged, PR #47) — all recovery in **`@marid/channel-client`** (shared → PH-7 inherits) + one non-admin route in marid-auth; additive, **zero upstream edit, no P-\***. **(a) reconnect:** the firehose pump reconnects on drop with capped exponential backoff (500ms–30s), first subscribe retried (server-not-up now recoverable); a per-connection controller ends the pump on shutdown OR a deliberate re-subscribe (pump races `next()` against the abort, so it never hangs on a stream that ignores the signal); `done` resolves ONLY on shutdown. **(b) re-fetch recovery:** on reconnect the OWNED tracked sessions re-read the durable store (`session.messages`, no limit → full history) and flush the latest assistant message text **edit-in-place** (same `part.id` → same channel message; identical text skipped). `part.id`-keyed = exactly what Marid's live `message.part.updated` renders on (the v2/next `session.next.text.*` family is not built), so a turn finished during the gap renders **once, no duplicate**. **BOUND (non-owned) sessions are NEVER re-fetched** — `session.messages` is owns-gated (403, INV-001/EXP-008) → they resume live only, gap frames lost (documented; the read gate is **not** widened). **(c) attach-triggered mid-stream reconnect (cross-process):** the admin `/marid/bindings` is off-limits to a channel token, so a new **non-admin `GET /marid/self-bindings`** returns the AUTHENTICATED token's own bound sessions (keyed on the token, not a `?token=` param → spoof-proof; INV-001-safe — leaks nothing the owns∪bound firehose already grants; WRITE/attach stays admin-only). The channel-client polls it (default 45s; an intentional poll-triggered re-subscribe skips backoff+refetch so the attach mirrors instantly) and re-subscribes on any set change (attach OR detach) → the server re-applies owns∪bound fresh. **Advisor-caught & fixed:** a poll-abort mid-subscribe retries (not exits, else mirroring dies); the injected gateway sleep is NOT abort-aware, so the internal sleep always races shutdown (else `done` hangs up to `bindingPollMs` on SIGINT — biting test added). channel-client **11→16**, marid-auth **119→121**, marid-telegram **90→91** green; typecheck+lint clean. **AC verdicts unchanged** (AC-019 Partial / AC-024 Met — 6.5 is FR-036/043 + RISK-006, not an AC flip; the live E2E that flips AC-019 is WBS-6.6). **Merged to develop (PR #47).** |
| WBS-6.6 | **Test strategy (4 tiers)** — fake-server E2E (blocking PR gate, extended) + GramJS userbot test-DC (TEST-TG-E2E) + Telegram-Web-Playwright (TEST-TG-UI) [local-pre-PR + GitHub-on-demand] + native mobilewright (TEST-TG-MOBILE, manual) + mirroring sync tests (TEST-SYNC) + **blast-radius/degradation tests** (AC-024, TEST-AUTH/CONTRACT); EXP-007/008/009/010 | EXP-007/008/009 PASS; AC-017/019/020/021/024 green; TEST-AUTH/SEC/CONTRACT green | AC-017/019/020/021/024, ADR-0013, RISK-024, EXP-007/008/009/010 | ✅ done (merged, PR #48) — **DELIVERED:** fake-server E2E extended (SSE-drop recovery + inbound/outbound files); **TEST-TG-E2E** userbot live vs the real gateway (`scripts/tg-userbot-e2e.ts`, model-free slash round-trip + deny-by-default; DEP-014 resolved) → **AC-020 Met**; **TEST-SYNC** live bidirectional mirror + unattached-invisible + attach re-subscribe (`scripts/tg-model-e2e.ts`, real GLM over real MTProto) → **AC-019 Met**; blast-radius/degradation + TEST-AUTH/SEC/CONTRACT → **AC-024 Met**; live text round-trip + **live tool calling** → **AC-017 Met (2026-07-12)** — the two "live-impossible" parts resolved: the gateway's `promptAsync` (which forks the turn off its request scope → ZERO tools) was replaced by the sync `session.prompt` route driven detached (`gateway.ts`, Marid-side, no upstream edit), so tools + MCP resolve; **inline keyboard proven LIVE** (`scripts/tg-tool-e2e.ts` — real GLM bash call → `[✅ Approve\|🚫 Deny]` → Approve → tool runs), and **outbound files send as multipart bytes** (`data:` file parts decoded + uploaded; `gateway-integration` + unit tests). Channel tool policy = agent-config ruleset (`docs/execution/telegram-channel-tools.md`); **TEST-TG-UI** Telegram-Web-Playwright render tier live vs production (`scripts/tg-web-e2e.ts` Bun + `scripts/tg-web-driver.mjs` Node — real `telegramify-markdown` → real Bot API → web.telegram.org rendered DOM: `<strong>`/`<code>`/`<pre>`/no-literal + `<img>` media; 4/4 stable) → **AC-021 Met (2026-07-12; [EXP-009](../experiments/exp-009-report.md) PASS)**. **🔒 INV-001 firehose isolation leak found via the live tier & FIXED:** [ADR-0016](../adrs/adr-0016-sse-isolation-route-not-header.md) (`isStream` recognises firehose routes by pathname — the SDK omits `Accept: text/event-stream`, which had bypassed the owns∪bound filter for all non-admin tokens) + [ADR-0017](../adrs/adr-0017-firehose-own-session-lazy-visibility.md) (lazy own-session visibility — the companion defect the leak had masked); [EXP-015](../experiments/exp-015-report.md)/[RISK-025](../risks/risk-register.md); real-request regression test; marid-auth **123** green; zero upstream edit, no `P-*`. EXP-007/008/009 PASS. **AC-017/019/020/021/024 all Met.** **Merged to develop (PR #48 `4409d92f`) → MS-007 met (2026-07-12).** Deferred (not MS-007 blockers): native-mobile TEST-TG-MOBILE (EXP-010, manual/never-a-gate); GitHub on-demand TEST-TG-UI/E2E wiring. |
| WBS-6.7 | **Docs + diagrams** — update `architecture/api-event-contract.md` (attach/binding/mirror endpoints + events + fan-out `seq`/`stateVersion` + contract version bump; TEST-CONTRACT pins them), `architecture/architecture.md` (Marid Gateway + `@marid/channel-client` + mirroring in the target arch; **patch-surface register** — enumerate the one gateway `Server.extend` hook if it becomes a `P-*`), the **Tarseem OpenCode+Marid diagrams** (`architecture/diagrams/` — gateway, channel-client, mirroring, remediated Telegram), and root docs (README / CLAUDE.md / AGENTS.md / CODEMAPS) | contract + architecture + diagrams + READMEs reflect PH-6; `validate_package.py docs/` = OK | FR-034/035, NFR-010, ADR-0011/0012 | ✅ done (merged, PR #48) — PH-6 docs closed. `api-event-contract.md` **v1.2** (four `/marid/*` routes; `owns ∪ bound` mirroring on `/event`+`/global/event`; route-not-header SSE isolation ADR-0016/0017; tool calling via sync `/session/{id}/message` detached + outbound multipart — all *behaviours*, **no new replay path**, consistent with the v1.1 re-fetch correction). `architecture.md` **v1.1** (new *Marid Gateway & cross-surface mirroring* section — gateway, `@marid/channel-client`, mirroring; **zero new P-\***; stale seq-replay §7 line corrected to re-fetch). New Tarseem **20-gateway-mirroring** diagram (spec + PNG/SVG + README row). Stale `ci.yml` reconnect comment + CLAUDE.md/AGENTS.md "PH-4 next" pointers corrected. `/marid/self-bindings` pinned in the merged-`/doc` test (`marid-auth` gateway.test, 10 pass). `validate_package.py docs/` = OK. Root docs (README/CODEMAPS) flagged to operator, not expanded (checkpoint). **Merged to develop (PR #48 `4409d92f`) → MS-007 met (2026-07-12).** |

## PH-7 WhatsApp adapter — post-MVP (reuses the PH-6 gateway + `@marid/channel-client` + mirroring; ADR-0010/0014/0015 Proposed; not-started, operator-gated)
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-7.1 | `@marid/whatsapp` package skeleton — separate process; `channel:` token; config + allowlist + dedup | Package builds; connects to `marid serve` with a channel token | FR-045, ADR-0005/0010 | not-started |
| WBS-7.2 | WhatsApp client integration (WAHA-NOWEB-WS primary / Baileys-direct-hardened alt); outbound-only; auth-state persistence | Inbound/outbound round-trip; outbound-only verified (no inbound port) | FR-047, OQ-004, RISK-014 | not-started |
| WBS-7.3 | Streaming simulation (presence + throttled edit-coalescing) + media send/receive | Streaming within WhatsApp limits; media tests | FR-048/049 | not-started |
| WBS-7.4 | Identity allowlist + inbound dedup + capability policy + **token-bound text-reply permission UX** (ADR-0015; strict parse, single-use/JID/TTL, server-side scope re-check — no interactive buttons) + **attach/mirror** (reuse ADR-0012) | Stranger ignored; bound agent; `APPROVE <token>` parser rejects ambiguous/wrong-token/expired; act-via-ownership; policy-denial tests | FR-028/050/051/052, INV-001, ADR-0012/0015, RISK-021 | not-started |
| WBS-7.5 | **Test strategy (ADR-0014)** — fake-WA at the WAHA WebSocket boundary (**deterministic blocking gate**, TEST-WA/AC-023) + burner real-protocol probe (manual, EXP-012) + native-app mobilewright (manual, EXP-013); narrow WAHA interface so the fake is small; EXP-006/011 | EXP-011 PASS (fake-WA gate); AC-018/022/023 green; burner/native probes documented | AC-018/022/023, EXP-006/011/012/013, ADR-0014, RISK-022 | not-started |
| WBS-7.6 | **Docs + diagrams** — update `architecture/api-event-contract.md` + `architecture/architecture.md` (WhatsApp adapter + WAHA sidecar + token-text permission + narrow interface), the Tarseem **diagrams** (`architecture/diagrams/` — add the WhatsApp channel), and root docs (README / CLAUDE.md / AGENTS.md / CODEMAPS) | docs + diagrams reflect PH-7; `validate_package.py docs/` = OK | FR-035, NFR-010, ADR-0010/0014/0015 | not-started |

## PH-8 Isolation & deep rebrand — post-release (total DATA isolation from co-installed OpenCode + deep rebrand + upstream sync; ADR-0018 + DEC-022..027 Proposed; not-started, operator-gated)
Sub-phases map to the twice-reviewed plan §4 (Phase 0..7). Each leaf = branch → PR into `develop` → CI green → **operator merge** (INV-005); trackers + `validate_package.py docs/` updated in the same change.
| WBS | Item | DoD | Traces | Status |
|---|---|---|---|---|
| WBS-8.0 | **Keystone scoping + ADR** (docs-only) — open PH-8/MS-009 (roadmap/WBS/milestones) + AC-025..031; author **ADR-0018** (data isolation, kept-env + pierce disclosure, config filename/fallback, `.opencode` kept, one-time-copy migration, no DB rename, full identity transform, two-tone wordmark + web scope, pre-registered P-6/P-7/P-8 + P-2 expansion) + DEC-022..027 | ADR-0018 Proposed at the operator gate; all PH-8 trackers open; `validate_package.py docs/` = OK; **nothing code touched** | ADR-0018, DEC-022..027, all 6 reported issues | 🔄 this PR (docs-only) — **STOP at operator gate before any code phase** |
| WBS-8.1 | **Upstream sync FIRST** — `upstream-baseline/2026-07-03-eb3476660` → `upstream/dev cf7503687a` (167 commits / 458 files; zero renames; binary-side churn low, `packages/app` high) | Delta report reviewed (RISK-001); conflicts resolved on existing P-2/P-3/P-CI (Marid wins); 3-OS build + contract green; later phases rebase + re-enumerate | FR-061, RISK-001, ADR-0001 | not-started |
| WBS-8.2 | **Total DATA isolation** (dirs + config; env kept) — build-time `__MARID_APP` define + `global` app-name (P-6); config `marid.json` primary + project-level `opencode.json` fallback, no global fallback (P-7); `autoupdate:false` + env; one-time-copy migration; align `marid-instance` (`paths.ts` + tests) | Built-binary effective-read-path assertions (data/state/config; fresh auth; coexistence; plugin-compat; **negative env cases**; **v0.2.0-upgrade case**); instance/gateway/telegram suites green | AC-025/026/027/031, DEC-022..025/027, P-6/P-7 | not-started |
| WBS-8.3 | **Agent identity** (full scope) — Marid-owned transform wrapping `provider()` output in `session/system.ts`: identity → Marid; self-doc-fetch → Marid docs/neutralized; support URLs → Marid repo; **CI guard** (no emitted prompt matches `\bopencode\b` outside allowlist) | Identity Q&A = Marid; CI guard green; regression "what can you do / where do I report bugs" | AC-028, DEC-026, P-8 (conditional) | not-started |
| WBS-8.4 | **Complete TUI rebrand** (post-sync re-enumerate) — exit logo + `marid -s` (`util/presentation.ts`); sidebar footer "● Marid {version}"; `attention.ts`; update toast; **remove GO upsell** (`logo.ts`); startup brand flame + **two-tone wordmark** (`logo.ts`/`logo.tsx`/`cli/ui.ts`) behind a render gate (incl. 256-color) | TUI surfaces = Marid; no popup; render gate incl. 256-color screenshot; P-2 register expanded | AC-027/029, P-2, issue-3 | not-started |
| WBS-8.5 | **Web UI rebrand** (separate PR; high sync-churn — re-enumerate) — favicon set/PWA icons/social-share from the flame; `ui/components/logo.tsx` `Mark`+`Splash`; local notification icon (drop `opencode.ai` remote); release-notes popup repoint/disable | Web favicon/splash/Mark/notif/release-notes = Marid; no `opencode.ai` fetch; does not gate the v0.3.0 TUI release | AC-030, P-2, issue-5 | not-started |
| WBS-8.6 | **Docs + diagrams + screenshots + trackers** — README (marid dir, config name, kept-env + pierce table pointer, isolation/coexistence, new screenshots), CONTRIBUTING, `usage.md` (dirs per OS, config, isolation, migration, env-pierce table), branding.md (two-tone + web + exit/footer surfaces), architecture.md (all new P-* rows + reconcile recipes), CLAUDE.md/AGENTS.md, Tarseem 13/05/01 (+optional isolation diagram) re-rendered (G-IDS), screenshots | docs/diagrams/screenshots reflect PH-8; `validate_package.py docs/` = OK; full Keystone reconcile | AC-025..031, NFR-010, ADR-0018 | not-started |
| WBS-8.7 | **Release v0.3.0** — `develop → main` sync PR (merge-commit) → `v0.3.0` tag (`gh api` refs) → `marid-release` → 21 signed assets + 3-OS install-smoke (`--version` + smoke asserts marid dirs) | Public `v0.3.0` released; 3-OS install-smoke green; all 6 reported issues map to a passing check (matrix row H) | AC-025..031, FR-060/064, MS-009 | not-started |

---
status: Approved
version: 1.1.2
updated: 2026-07-21
owner: operator (STK-001)
---

# Deferred-Work / Tech-Debt Register

Durable index of known-not-done and accepted debt (distinct from the forward roadmap). Local `#` ids, no
governed prefix. Severity Low/Med/High. Status Open / In progress / Done / Won't do.

| # | Item | Severity | Invariant / req at stake | Proposed (additive) fix | Acceptance / guard | Status |
|---|---|---|---|---|---|---|
| 1 | AC-007 criterion assumes `?after=<seq>` SSE replay that the v1 firehose does not provide | Med | FR-036 | Supersede AC-007 with a re-fetch-recovery criterion (new `AC-`), pointing to AC-008; keep AC-007 as history | **Resolved 2026-07-16** — no new `AC-` was minted: [AC-008](../validation/acceptance-criteria.md) already *is* the re-fetch-recovery criterion (server killed mid-prompt → clients reconnect → history intact, session usable per documented recovery behavior) and is Met, so a new criterion would duplicate it. AC-007 kept as history, verdict **Superseded** in the [acceptance audit](../validation/acceptance-audit.md); traceability points recovery at AC-008. Deviation from the proposed fix (which assumed a new `AC-`) recorded here | Done |
| 2 | `POST /permission/:requestID/reply` not ownership-gated (opaque `per_` id) | Low | INV-001, FR-028 | In-pipeline requestID→session map to gate reply by owner | reply-gating test; `GET /permission` already filtered so ids aren't discoverable via API. **Channel half CLOSED 2026-07-19 ([ADR-0020](../adrs/adr-0020-channel-non-session-deny-by-default.md)):** the flat reply is a non-session POST, now denied for `channel:` scope by the deny-by-default non-session allowlist (channel uses the ownership-gated session-scoped reply route). **Residual (Open):** `client` scope keeps blanket non-session ALLOW, so a client replying to another client's `per_` id is still ungated — needs the requestID→session map. | Open (client half) |
| 3 | Cosmetic branding P-2 (README / TUI title / user-agent / logo) deferred | Low | FR-065, DEC-013 | Land in PH-5 (WBS-5.4) alongside README + logo | hygiene grep + visual check. **Done — README + TUI title + flame logo shipped at PH-5 (WBS-5.4), deepened at PH-8 (WBS-8.4/8.5: two-tone wordmark, web favicon/PWA/Mark/Splash); AC-029/030 Met. User-agent intentionally kept upstream (dropped from P-2, NFR-001) — a decision, not a gap.** | Done |
| 4 | Config defaults P-3 (`lsp:false` etc.) deferred | Low | NFR-002, DEC-013 | Ship distribution config defaults in PH-5 (prefer config file over code) | build smoke; patch-surface stays enumerable. **Done — P-3 applied WBS-5.4 (2026-07-08): instances launch with `lsp:false` via an `OPENCODE_CONFIG_CONTENT` layer (marid-instance), operator-overridable; additive, no upstream edit (see architecture.md P-3).** | Done |
| 5 | Deferred (Full) requirements FR-037, FR-044, FR-058 (FR-047 → **Met** via PH-7/ADR-0010, AC-018, 2026-07-18 — dropped from this list) | Low | scope | Post-MVP; live in [functional](../requirements/functional.md) with triggers; marked `Scope: Full` in the matrix | revisit at their triggers (FR-047 delivered) | Open |
| 6 | `deviation-branch-protection` (GitHub Free blocked private branch protection) | Med | INV-003 | Resolved by DEC-010 (repo made public); WBS-5.1 "private releases" premise to reconcile in PH-5 | **Closed 2026-07-17 (bookkeeping — both halves were done long before).** (a) *Protection*: DEC-010 Approved 2026-07-03 made the repo public, unblocking rulesets — protection is live on `main`+`develop` (17 required checks; WBS-0.3 DoD "required checks enforced" ✅). (b) *The WBS-5.1 reconciliation*: the "private releases" premise was reconciled **to public** at **MS-006 (2026-07-09)** — [AC-014](../validation/acceptance-audit.md) reads public/anonymous and is **Met** on the public `v0.1.0`. The `In progress` status was stale by ~8 days. Source doc [deviation-branch-protection](../decisions/deviation-branch-protection.md) flipped Proposed→Superseded in the same change | Done |
| 7 | FR-030 in-pipeline trace correlation (request-ID → traces) left for the pipeline | Low | FR-030 | Wrapper does request-ID + audit; deeper trace correlation is an in-pipeline follow-up | correlation test | Open |
| 8 | Telegram gateway firehose `pump` has **no reconnection** (`packages/marid-telegram/src/gateway.ts:181-189`) — one SSE drop stalls the reply path permanently; contradicts RISK-006's stated "reconnect via authoritative-store re-fetch" mitigation. Surfaced PH-5 as an intermittent `marid-telegram (windows-latest)` hang (~100s, no reply). | Med | FR-036/043, RISK-006 | Add firehose reconnect + authoritative-state re-fetch on drop (contract v1.1 recovery). Interim (PH-5): bounded 3× retry-wrapper on the live E2E in `ci.yml` (operator-chosen); deterministic AC-010/011/012 coverage unaffected (router/stream/permission/gateway-integration tests). | Retry-wrapper green on the RC; a reconnect test when the fix is built. **Done — WBS-6.5 (PR #47 squash `837fb70c`) explicitly folds #8: firehose reconnect (capped backoff 500 ms–30 s) + re-fetch-on-reconnect landed in `@marid/channel-client`, which marid-telegram consumes (the hand-rolled pump was replaced in the PH-6 extraction); reconnect tests live in channel-client. The `ci.yml` 3× retry-wrapper stays as a cold-start backstop.** | Done |
| 9 | Telegram gateway UX defects found in live E2E (2026-07-09): Markdown un-rendered (`stream.ts` HTML-escapes then sends `parse_mode:HTML`, but the agent emits **Markdown** → literal `**`/backticks); inbound media not landed into the agent's workspace (agent replies "image not found"); slash commands not routed; multi-part replies concatenated into one message; web→Telegram not mirrored (request/response bridge by design). The round-trip itself is verified working (operator "hello" → agent reply synced to web). | Med | FR-036/043, FR-065, INV-001 | **Post-MVP (operator-approved 2026-07-09):** replace the hand-rolled gateway with a fork/port of `grinev/opencode-telegram-bot` (MIT, grammy + remark markdown + official `@opencode-ai/sdk`); INV-001 preserved by **server-enforced `channel:` token** (the fork authenticates to a Marid instance; marid-auth does deny-by-default + bound-agent) + the fork's own allowlist. See **[ADR-0008](../adrs/adr-0008-telegram-gateway-fork.md)**. v0.1.0 ships Telegram **beta** with these documented. | Fork target evaluated (MIT / 897★ / grammy / official SDK); acceptance = markdown+media+slash parity AND INV-001 held (channel-token auth + allowlist), verified against a real provider | Done — **the deferred work (fix the 5 UX defects) is delivered**, though *not* by the proposed fix: the PH-6 gate (2026-07-10) approved **fix-in-place** ([ADR-0009](../adrs/adr-0009-telegram-fix-in-place.md) supersedes the fork ADR-0008), so the defects are fixed in the Marid-owned gateway, not a fork. Closed at MS-007 (PR #48 `4409d92f`) — AC-017 Met |
| 10 | No automated usage/download-stats mechanism for the single-operator distribution. Upstream's `STATS.md` (npm + GitHub download table) was removed as inapplicable; releases are public (DEC-010) so real per-asset counts are available ad-hoc via `gh api repos/A-H-911/marid/releases`. | Low | — | Post-MVP: a scheduled Marid-owned job pulls GitHub Releases download counts and appends to `STATS.md` (sync-durable). | `STATS.md` shows real per-tag download counts, refreshed on a schedule | Open |
| 12 | **WhatsApp outbound dead-letter = log-and-drop, no queue** (`packages/marid-whatsapp/src/gateway.ts` — an outbound send that fails is `.catch`→`log`ged and dropped). FR-051 asks for dead-letter handling; the shipped ceiling is a named `ponytail:` simplification (single-operator, human-paced channel — a lost outbound is visible to the operator in the chat). | Low | FR-051 | Add a durable outbound retry/dead-letter queue **only if** a real workload shows dropped sends matter; until then the log line is the whole record. | A bounded outbound queue with a replay/inspect path, if ever justified | Open |
| 11 | **CI flake — upstream test `packages/core/test/process/process.test.ts` "captures stdout and stderr in emission order".** The child script writes `out 1` immediately, `err 1` at 10ms, `out 2` at 20ms; the test asserts the `combineOutput` result is exactly `"out 1\nerr 1\nout 2\n"`. On a starved ubuntu runner the parent's stderr reader is scheduled late, so the observed order flips to `"out 1\nout 2\nerr 1\n"`. Same class as the `shell.test.ts` entry already recorded under P-CI-3 (reader starvation, **not** a timeout). | Low | — | Widen the two **in-script constants** (`10`/`20` → e.g. `100`/`200`) with a `marid:` comment, per the `shell.test.ts` precedent in [P-CI-3](../architecture/upstream-sync-strategy.md); it becomes a new P-CI-3 row when applied. **Note:** CLAUDE.md's "route new timing flakes through `OPENCODE_TIMING_SCALE` (P-CI-4)" does **not** apply as written — `it.effect` is called with no `opts`, so there is no per-test budget to scale. Deliberately not pre-fixed: see the P-CI-4 watch-list doctrine (fix on recurrence, don't accumulate upstream-edit surface). | Two consecutive green `unit (ubuntu-latest)` runs after the widening; entry moves to P-CI-3 | Open |
| 13 | **Channel UX — an inbound non-approval message while an approval is pending gets no ack.** Observed live ([EXP-012](../experiments/exp-012-runbook.md), 2026-07-20, WhatsApp): with a `bash` approval pending, the operator's "yes" (and a wrong token) drew **no response**; the run stays **suspended** on the permission gate until `APPROVE`/`DENY` or the permission timeout auto-denies (`"Timed out — denied"`). Question parked for later: is silent-drop the intended behaviour, or should the gateway **ack** ("still waiting on APPROVE `<token>`") or **queue** the message? Applies to Telegram too. | Low | FR-028/049, INV-004 (UX only; the security gate is correct) | Investigate the run-loop suspension + gateway `onReply` path; decide ack-vs-queue-vs-drop; if a change is wanted, do it channel-symmetrically. | **Decided (WhatsApp): ACK without swallowing.** `permission.ts onReply` now sends "Still waiting on your approval…" when a non-approval message arrives with an approval pending, and still returns `false` so normal chat flows on (never swallows, never approves — INV-004 intact; the run stays suspended by design). Tests in `permission.test.ts`. The `onReply`-first flow was found **deliberate** (don't hijack normal chat), so the gap was the missing feedback, not the flow. | In progress — WhatsApp done; Telegram symmetric follow-up |

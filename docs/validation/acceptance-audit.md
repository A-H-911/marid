---
status: Draft
version: 0.1.0
updated: 2026-07-14
owner: operator (STK-001)
generation: derived
---

# Acceptance Audit (verdict × evidence per AC-)

**Derived — regenerated at every phase gate.** The execution agent's during/after close-out of
[acceptance-criteria](acceptance-criteria.md): each `AC-` carries a verdict `Met | Partial | Not-met |
Pending` with evidence. Distinct from the planner's pre-handoff readiness report. Gate **G-PROGRESS**
checks that every `AC-` is represented here. Reflects real state through **PH-5 (MS-006, 2026-07-09)**:
the public `v0.1.0` release closed AC-014; AC-015 closed at the sync cycle (#31).

| AC | Criterion (short) | Verdict | Evidence | Notes |
|---|---|---|---|---|
| [AC-001](acceptance-criteria.md) | Instance add/start → own tree + health | Met | MS-003 (PR #17): `marid instance add/start`, allocated port, 0700 tree, health-gated; 3-OS `marid-isolation` green | PH-2 |
| [AC-002](acceptance-criteria.md) | Two instances concurrent → no cross-write | Met | MS-003: live 2-instance diff `instance-isolation.test.ts` — distinct ports, per-instance DB/sessions, no escape from composed XDG roots | PH-2 · NFR-008 |
| [AC-003](acceptance-criteria.md) | No token → 401 + audit | Met | MS-002 (PR #13): `@marid/auth` 401 unauthenticated; audit JSONL line written | PH-1 |
| [AC-004](acceptance-criteria.md) | client-scope crosses session/admin → 403 + audit | Met | **⚠ Live regression found → ✅ FIXED same-day (2026-07-11, [EXP-015](../experiments/exp-015-report.md) / [RISK-025](../risks/risk-register.md), [ADR-0016](../adrs/adr-0016-sse-isolation-route-not-header.md)):** the WBS-6.6 live tier found the client **EVENT (SSE)** isolation was defeated on the real path — `/event`+`/global/event` served UNFILTERED to a non-admin token whose SSE request omits `Accept: text/event-stream` (the SDK omits it), because `isStream()` gated the owns∪bound filter on that header (the PR #15 unit suite tested only header-ful frames). **Fix:** `isStream()` now recognises the firehose routes by **pathname** (header-independent); pinned by a real-request-level regression test (`global-event-filter.test.ts` PART 2b) + `global-event-isolation-repro.ts` (all 3 modes isolated). Route-ownership 403s unaffected throughout (MS-002 + DEC-011). marid-auth **123** green. | PH-1 |
| [AC-005](acceptance-criteria.md) | Over rate limit → 429 + SSE cap | Met | MS-002: token-bucket 10/s burst 30, SSE cap 4, 429 + retry-after | PH-1 |
| [AC-006](acceptance-criteria.md) | SDK-created session visible + live in TUI/subscriber | Met | MS-004 (PR #19): §7 KPI-001 E2E — 2nd client discovers+continues, subscriber sees both | PH-3 · KPI-001 |
| [AC-007](acceptance-criteria.md) | Reconnect with `?after=<seq>` replays missed events exactly once | Not-met | Premise invalid: contract v1.1 (PH-3) corrected the `?after=` per-session replay claim — the v1 firehose is live-only. Recovery is authoritative-store re-fetch, verified under AC-008 | Criterion superseded by the v1.1 correction; see [deferred-work-register](../execution/deferred-work-register.md) |
| [AC-008](acceptance-criteria.md) | Kill + restart mid-prompt → history intact, usable | Met | MS-004: kill+restart E2E — reconnecting client re-fetches authoritative history written before restart, no state loss | PH-3 · replaces AC-007's replay premise |
| [AC-009](acceptance-criteria.md) | Two simultaneous prompts → documented queue/steer, no corruption | Met | MS-004: concurrency E2E through the authed wrapper (join/steer, one Runner, no corruption); contract v1.1 Concurrency section | PH-3 · EXP-001 |
| [AC-010](acceptance-criteria.md) | Non-allowlisted Telegram user → ignored + logged | Met | MS-005 (PR #23): allowlist + `update_id` dedup; stranger ignored + logged, proven live in the 3-OS `marid-telegram` TEST-TG job | PH-4 · FR-050 |
| [AC-011](acceptance-criteria.md) | Allowlisted ask → progressive edits, 4096-split, complete | Met | MS-005 (PR #23): HTML/4096-split edit-coalesced streaming (EXP-003 cadence); progressive edits + complete reply proven live in the 3-OS `marid-telegram` TEST-TG job | PH-4 · KPI-002 |
| [AC-012](acceptance-criteria.md) | Policy-denied tool → inline-keyboard permission; deny blocks | Met | MS-005 (PR #23): permission round trip proven via faked-SDK integration test (event→keyboard→Deny→`permission.respond(reject)`) + `parseAskEvent` schema lock + `permission.test` (claim/timeout/races) + marid-auth `channel-binding`/`scope` (INV-001: channel token cannot reach /shell or /command, cannot escape its bound agent). The live LLM-tool→permission link is unreachable in-harness (the opencode HTTP-served run resolves zero tools — a harness limit, not a gateway/provider defect) | PH-4 · INV-001 |
| [AC-013](acceptance-criteria.md) | `marid` profile build → excluded surfaces absent + hygiene grep | Met | MS-002: additive `src/marid.ts` + `script/marid-build.ts`; 3-OS `marid-build` green; hygiene grep passes | PH-1 · ADR-0002 |
| [AC-014](acceptance-criteria.md) | Tagged release → public signed binaries+checksums, 3-OS install (DEC-010: public/anonymous) | Met | **Public `v0.1.0` release cut (2026-07-09)** on `main` merge `8bf4ab61e`: `marid-release.yml` published 7 targets × (archive + `.sha256` + `.minisig`) = 21 signed/checksummed assets, release not draft/prerelease. **Install-smoke** proves the anonymous download→`minisign -Vm`→`sha256sum -c`→run path on **Linux + Windows** (both green); **macOS** smoke was a matrix filename typo (`.tar.gz`→`.zip`), fixed forward in PR #38 — the darwin `.zip` asset is present + signed like the others. KPI-006: RC (#35→main) 17 checks green. | PH-5 · KPI-006 |
| [AC-015](acceptance-criteria.md) | Upstream sync PR → contract/migration/delta + one real cycle | Met | `marid-sync-upstream.yml` (PR #28) provides the weekly conflict-check + monthly merge PR carrying the delta report, migration-review, and dependency-diff; **one real 91-commit cycle merged via merge-commit (PR #31), `upstream/dev` now an ancestor of develop, ≤1 person-day (KPI-004).** | PH-5 · KPI-004 |
| [AC-016](acceptance-criteria.md) | Secret would appear → redacted | Partial | Corrected by PH-4 audit (2026-07-07). MET slice: the audit stream never carries the bearer (logs the token *name*, no request bodies) and secrets live only in env / sha256-hashed stores — `marid-auth` `audit.test.ts` proves the 0600 append + field shape (NOT redaction); the Telegram **bot-token literal** is masked in gateway logs (`marid-telegram` `redact.test.ts` + `safeLog`). OPEN slice → **PH-5**: no configured-secret-value redactor on channel egress (`stream.ts`), on general logs/errors (no runtime facility), or on session export (`marid export` raw by default). Secret-in-egress is contained in the MVP by the B2/B4 authorization boundary (restricted agent cannot read `auth.json`). Disposition: ADR-0007 (Approved); redactor tracked to PH-5/WBS-5.1. | PH-1 (partial) · PH-5 (redactor) · RISK-007 |
| [AC-017](acceptance-criteria.md) | Telegram remediation parity + INV-001 | Met | **WBS-6.2 impl done (2026-07-10).** EXP-005 PASS; 4 UX defects fixed in place — MarkdownV2, files both ways, deny-by-default slash whitelist, multi-part separation; INV-002/004 held; INV-001 acting stays `@marid/auth`-enforced. **LIVE (2026-07-11):** a real GLM reply round-trips Telegram→model→Telegram through the real gateway (`scripts/tg-model-e2e.ts`); slash + non-allowlisted-ignored + INV-001 refusal live (AC-020); SSE-drop reconnect+refetch (WBS-6.5). Markdown *render* fidelity proven live by **[AC-021](acceptance-criteria.md) → Met**. **→ Met (2026-07-12):** the two prior "live-impossible" parts are now resolved. **Root cause found + fixed:** the gateway drove turns via `sdk.session.promptAsync`, whose handler forks the turn off its request scope (`Effect.forkIn`) so it resolved a ZERO toolset — the bot could never use tools. The gateway now drives the sync `session.prompt` route detached (`gateway.ts` onMessage; Marid-side, no upstream edit) → tools resolve. **Inline keyboard proven LIVE** (`scripts/tg-tool-e2e.ts`, real GLM over real MTProto): a real bash tool call surfaces `[✅ Approve \| 🚫 Deny]` in Telegram, Approve is tapped, the tool runs, the turn completes. **Outbound files** now send as **multipart bytes** (assistant/tool `data:` file parts decoded + uploaded — `bot-api`/`media`/`gateway.ts`; deterministic `gateway-integration` + unit tests). Tool calling + MCP ride the same server path + agent-config ruleset (`docs/execution/telegram-channel-tools.md`; deterministic proof `telegram.test.ts` live-keyboard test + `step0-tools-probe.test.ts`). marid-telegram 99 green. Operator-accepted (INV-005, 2026-07-12). **Landed on develop via PR #48 (`4409d92f`, 2026-07-12) → MS-007 met.** | post-MVP · PH-6 |
| [AC-018](acceptance-criteria.md) | WhatsApp round-trip + INV-001 + outbound-only | Pending | **PH-7, not-started.** Unofficial client per C-9 / R-12 (ADR-0010 Proposed); verified by EXP-006 (fake-WA) + AC-018 tests + PH-7-start live probe | post-MVP · PH-7 |
| [AC-019](acceptance-criteria.md) | Cross-client bidirectional mirroring + view-via-binding/act-via-ownership | Met | **⚠ Live regression found → ✅ FIXED same-day (2026-07-11, [EXP-015](../experiments/exp-015-report.md) / [RISK-025](../risks/risk-register.md) / [ADR-0016](../adrs/adr-0016-sse-isolation-route-not-header.md)):** the WBS-6.6 live tier REFUTED the "unattached session stays invisible / INV-001 leak closed" claim below — a channel token received+rendered a session it neither owned nor was bound to, because the owns∪bound filter was gated on the SDK-absent `Accept: text/event-stream` header (`isStream()`), so `/global/event` was served unfiltered (this had invalidated the live mirror-out proof; "mirror-in" was confounded via an admin observer). **Fixed** — `isStream()` is now route-based (`global-event-filter.test.ts` PART 2b + `global-event-isolation-repro.ts` green); the isolation mechanism holds live again (also [ADR-0017](../adrs/adr-0017-firehose-own-session-lazy-visibility.md) restored own-session visibility the fix had exposed). **Verdict → Met (2026-07-11):** the live re-run `scripts/tg-model-e2e.ts` — real GLM (OpenRouter) over the real gateway + real MTProto — PASSES all four: AC-017 text round-trip, **mirror-in**, **unattached-invisible** (the negative control — the leak fix holds live, so mirror-out is no longer confounded), and **mirror-out** (attach → WBS-6.5 self-bindings re-subscribe). marid-auth 123 / telegram.test 2 green. Prior deterministic mechanism evidence below is live-effective post-fix. — **Blockers cleared — WBS-6.1 slice b delivered the mechanism + operator-reachable attach; the real-account live bidirectional Telegram E2E (which flips this to Met) remains WBS-6.6 (2026-07-11).** Prior (WBS-6.3/6.4): durable `BindingStore` + binding-aware `isVisible` (`owns ∪ bound`) at `/event`; `mirroring.test.ts` proves mirror-in/out, explicit-attach scope, **act-via-ownership** deny (INV-001), degradation (RISK-024); first-responder-wins + permission.asked view proven. **slice b closes it:** (a) operator-reachable **attach** via the admin-gated `POST /marid/attach|/detach` + `GET /marid/bindings` (part 1); (b) the SAME `owns ∪ bound` filter now applied to `/global/event` — the firehose web + TUI + channel all ride — so a bound session's web/TUI turn reaches the channel and an **unattached session stays invisible** (`global-event-filter.test.ts`), while a wrapped-frame picker drops both the regular AND the durable `sync` twin of a non-owned session (INV-001 leak closed); (c) the channel-client lazily builds a streamer for a bound (non-owned) session with no prior `beginTurn`, Telegram rendering it into the operator's `defaultChatId` (`client.test.ts`, `gateway-integration.test.ts`). Real-account 3-OS Telegram bidirectional E2E = WBS-6.6 (`marid-telegram` job); **attach-triggered mid-stream reconnect now DELIVERED (WBS-6.5)** — a non-admin `GET /marid/self-bindings` poll re-subscribes the firehose on attach/detach so the server re-applies owns∪bound fresh (verdict stays Partial: still gated on the WBS-6.6 live E2E, not this mechanism). marid-auth 121 / channel-client 16 / marid-telegram 91 green. **Landed on develop via PR #48 (`4409d92f`, 2026-07-12) → MS-007 met.** | post-MVP · PH-6 |
| [AC-020](acceptance-criteria.md) | Userbot real-protocol E2E | Met | **EXP-007 PASS (2026-07-10) → LIVE against the REAL gateway (2026-07-11, TEST-TG-E2E, WBS-6.6).** `scripts/tg-userbot-e2e.ts`: a real GramJS userbot over real MTProto (prod DC) drives the REAL gateway (marid serve + channel token + `runGateway`, real Bot API) — `/help` → command list, `/frobnicate` → **deny-by-default refusal (INV-001)**, `/new` → new session. Model-free slash round-trip (no provider needed), deterministic; non-gating CI `marid-telegram-userbot.yml` (workflow_dispatch / label). DEP-014 (GramJS-on-Bun) resolved — runs live on Bun 1.3.14. **Landed on develop via PR #48 (`4409d92f`, 2026-07-12) → MS-007 met.** | post-MVP · PH-6 |
| [AC-021](acceptance-criteria.md) | Real-app GUI rendering (Telegram Web + Playwright) | Met | **LIVE (2026-07-12, WBS-6.6, TEST-TG-UI / [EXP-009](../experiments/exp-009-report.md)).** A headless Playwright driver logged into a real Telegram Web account (persistent `.pw-telegram/` profile) reads the **rendered DOM** of a bot reply and asserts real markup — bold→`<strong>`, inline code→`<code>`, fenced block→`<pre>` — and that **no literal `**…**` survives** (the direct guard on the ADR-0008 defect-1 regression: raw `**bold**` reaching the chat). Media half: a bot photo (`bot.sendPhoto`, public URL) renders as an `<img>`. Real path end-to-end: marid-telegram's real `telegramify-markdown` formatter → real Bot API → **production** `web.telegram.org` → asserted DOM. `scripts/tg-web-e2e.ts` (Bun: marid serve + fake LLM + gateway + real Bot API) + `scripts/tg-web-driver.mjs` (Node: Playwright — launch hangs under Bun); **4/4 stable runs**. **Deviations (operator-confirmed 2026-07-12, evidence notes not ADRs):** production Telegram not the test-DC/`?test=1` (as [EXP-007](exp-007-report.md) already superseded the test-DC premise); **local pre-PR** only — not wired into `ci.yml` (like the sibling userbot/model live tiers), GitHub on-demand deferred (ephemeral runner lacks the logged-in IndexedDB profile). The meaningful **outbound-file `onFile`** render stays deferred (zero-tools served-run + instance-local URL Telegram can't fetch, `gateway.ts:111`) — the media assertion uses the Bot API public-URL path, not `onFile`. Non-gating; INV-002 clean. **Landed on develop via PR #48 (`4409d92f`, 2026-07-12) → MS-007 met.** | post-MVP · PH-6 |
| [AC-022](acceptance-criteria.md) | WhatsApp token-bound text permission + act-via-ownership | Pending | **PH-7, not-started.** ADR-0015 (Proposed); verified by fake-WA (EXP-011) + parser unit test | post-MVP · PH-7 |
| [AC-023](acceptance-criteria.md) | WhatsApp fake-WA deterministic gate (round-trip/media/streaming/mirror/permission) | Pending | **PH-7, not-started.** ADR-0014 tier 2 (Proposed); verified by EXP-011 + TEST-WA | post-MVP · PH-7 |
| [AC-024](acceptance-criteria.md) | Gateway blast-radius isolation + degradation + new-endpoint contract | Met | **⚠ Live regression found → ✅ FIXED same-day (2026-07-11, [EXP-015](../experiments/exp-015-report.md) / [ADR-0016](../adrs/adr-0016-sse-isolation-route-not-header.md)):** the isolation sub-claim ("the owns∪bound filter on `/event`+`/global/event` bounds what a token can view") was defeated on the live path (filter gated on the SDK-absent `Accept` header); **fixed** by making `isStream()` route-based (regression test PART 2b + repro confirm). Contract/degradation/health sub-claims held throughout. — **WBS-6.1 slice b (2026-07-11).** **New-endpoint contract:** admin-gated `POST /marid/attach|/detach` + `GET /marid/bindings` served in the marid-auth wrapper, DOCUMENTED by merging a Marid-owned OpenAPI fragment into the intercepted `GET /doc`; pinned by TEST-CONTRACT (`gateway.test.ts`) **and** a live assertion in the `marid-sync` job (real gzip→strip→merge, `paths['/marid/attach'|'/detach'|'/bindings']`). **Blast-radius isolation:** non-admin token → 403 on the gateway routes (a `channel:` token cannot self-attach — INV-001); the `owns ∪ bound` filter on `/event`+`/global/event` bounds what a token can view. **Degradation:** EXP-008 + `mirroring.test.ts` (registry fault → owns-only, RISK-024). Health = existing `/global/health`. Additive: zero upstream edit, no `P-*` (EXP-014 PASS). **Landed on develop via PR #48 (`4409d92f`, 2026-07-12) → MS-007 met.** | post-MVP · PH-6 |
| [AC-025](acceptance-criteria.md) | Total DATA isolation — effective read paths are marid dirs only; no foreign auth/model bleed; co-installed OpenCode untouched | Met | **WBS-8.2 part 1 (P-6, PR #55 `a979a20e01`, 2026-07-13).** The app-name seam (`global.ts:10 const app = process.env.__MARID_APP ?? "opencode"`) isolates every machine-global dir at once (DB/auth/model/mcp-auth/config all derive from `Global.Path`). **Dev-proven:** `data-isolation.test.ts` (subprocess — `__MARID_APP=marid` nests data/config/state under `marid`; unset stays `opencode`, upstream regression proof) + live `instance-isolation.test.ts` (no leak into `~/.local/share/opencode`, tokens resolve). **Binary-proven:** the 3-OS `marid-build` isolation smoke (spawns the compiled binary under a throwaway XDG root, asserts the data dir lands under `marid`) went **green on all 3 OS** in #55 — closing the baked-define branch. | PH-8 |
| [AC-026](acceptance-criteria.md) | Env-pierce disclosure (WARN + disclosed behavior; kept-env still works) | Met | **WBS-8.2 part 2 (2026-07-13).** `src/marid-pierce.ts` `disclosePierce()` (called from `marid.ts` at boot) warns when any of `OPENCODE_CONFIG_DIR` / `OPENCODE_CONFIG` / `OPENCODE_CONFIG_CONTENT` / `OPENCODE_AUTH_CONTENT` / `OPENCODE_DB` is set — naming each active var + what it redirects OUTSIDE the marid dirs. The vars stay **honored** (disclosure, not enforcement — DEC-022 kept-env). `config-pierce.test.ts` (4) proves the naming, the no-pierce/empty-string no-op, and **INV-002** (the var value — incl. secret `OPENCODE_AUTH_CONTENT`/`OPENCODE_CONFIG_CONTENT` — is never printed). | PH-8 |
| [AC-027](acceptance-criteria.md) | No upstream update popup / no auto-update overwrite | Met | **WBS-8.2 (2026-07-13).** `marid-env.ts` sets `OPENCODE_DISABLE_AUTOUPDATE=1` (belt-and-braces bootstrap); the upgrade guard `cli/upgrade.ts:10` short-circuits on `Flag.OPENCODE_DISABLE_AUTOUPDATE`, so no version check / popup / self-overwrite path runs. marid also ships no `UpgradeCommand`. | PH-8 |
| [AC-028](acceptance-criteria.md) | Agent identity = Marid; no `\bopencode\b` in emitted prompts | Met | **WBS-8.2 Phase 3 (DEC-026, 2026-07-13).** `session/marid-identity.ts` `maridizePrompt()` rewrites the emitted system prompt at the single choke point (`session/system.ts provider()`, P-8): "You are OpenCode" → Marid, feedback repo `github.com/anomalyco/opencode` → the Marid repo, self-doc-fetch `opencode.ai/docs` → the Marid repo. App-name-gated (upstream `opencode` byte-unchanged). `agent-identity.test.ts` (19): identity/URL rebrand, `.opencode/` preserved (DEC-024), the opencode-app regression, and the **CI guard** — every shipped `prompt/*.txt` (read from disk, so sync-added prompts are covered) emits no `\bopencode\b` after the transform. | PH-8 |
| [AC-029](acceptance-criteria.md) | TUI startup/exit/footer/notif = Marid; no GO upsell | Met | **PH-8 Phase 4a+4b, 2026-07-14.** **4a (merged, PR #58 `a991ed7f62`):** every user-facing "OpenCode" string in the shipped `marid` binary → Marid across the main TUI (exit hint `marid -s`, notification title + sound-pack name, update toast, both sidebar footers "● Marid {version}", permission prose, ~12 tips' wrong-binary `opencode …`→`marid …` hints, model-error hints, crash screen + bug-report → Marid repo, docs link → Marid repo), the `--mini` run mode, and `uninstall.ts`; GO-upsell removed at root (TUI dialog subsystem deleted + server `retry.ts` message neutralized); both providers de-marketed (IDs kept). **4b (this PR):** §94 logo redesign — taller flame (`logo.ts`, 6-row, `leftCore` inner-highlight mask), flame gradient `#FBD24A→#F5901E→#DC2A16` + core `#FDEFB0→#F8B73C`, **two-tone split wordmark** blue `#2F6BFF` (MAR) / orange `#F0731F` (ID) behind a **truecolor render gate** (`supportsTrueColor()` via `COLORTERM`; single-tone `theme.text`/reset fallback on 256-color) — applied in both renderers (`component/logo.tsx` opentui RGBA + `cli/ui.ts` ANSI); splash badge → Marid flame (`cli/cmd/run/splash.ts`), then `go` glyph deleted. `logo.test.ts` (6) verifies the gate + equal-height/core-subset/split invariants; typecheck clean; presentation green. **Operator visual sign-off (truecolor + 256-color) is the merge gate.** | PH-8 |
| [AC-030](acceptance-criteria.md) | Web favicon/PWA/Mark/Splash/notif = Marid; no `opencode.ai` fetch | Pending | **PH-8 Phase 5 (web UI), separate PR.** | PH-8 |
| [AC-031](acceptance-criteria.md) | One-time migration from populated v0.2.0 — gateway tokens + Telegram pairing survive | Met | **WBS-8.2 (DEC-025, 2026-07-13).** `marid-migrate.ts` `maridMigrate()` — marker-triggered one-time copy of the pre-isolation OpenCode `data`+`state` (auth.json, `marid/` gateway tokens, `opencode.db`, `model.json`) into the isolated marid dirs; skips regenerable caches (`repos`/`log`). `data-isolation.test.ts` proves the copy, the regenerable-skip, marker **idempotency** (a file added post-run is not re-copied), and the fresh-machine no-op. **INV-002:** logs a count only — no file names or contents (asserted: `stderr` contains no secret token). | PH-8 |

## Summary

- **14 / 16 Met** (AC-001..006, 008..015), **1 Not-met** (AC-007 — premise superseded),
  **1 Partial** (AC-016 — redactor deferred, ADR-0007). AC-014 met at PH-5 (public `v0.1.0` release,
  KPI-006); AC-015 met at PH-5 (sync cycle #31, KPI-004). MS-006 gates on KPI-004∧005∧006 + accepted
  readiness report (execution gate 14) — **not** "every AC Met" — so the one Partial (AC-016) + one
  Not-met (AC-007) are disclosed residuals, not blockers.
- **Residual honesty:** AC-007 will be formally superseded (re-fetch recovery is the delivered behavior);
  tracked in the [deferred-work register](../execution/deferred-work-register.md).
- **Post-MVP additions (2026-07-09):** **AC-017 (Telegram, PH-6)** and **AC-018 (WhatsApp, PH-7)** are **Pending**
  (not-started; ADR-0009/0010 Proposed, operator-gated). MVP acceptance is unchanged (14/16 Met + 1 Partial +
  1 Not-met); the two new criteria are post-MVP and do not affect the accepted MS-006 verdict.

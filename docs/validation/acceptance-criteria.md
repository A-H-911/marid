---
status: Proposed (approved with gate 13 handoff)
version: v0.1
updated: 2026-07-09
owner: operator (STK-001)
---

# Acceptance Criteria (AC-)

Given/When/Then form; each AC is verified by the TEST- family named and rolls up to a KPI/milestone.

| ID | Criterion | Verifies | Tested by |
|---|---|---|---|
| AC-001 | Given a fresh machine with the Marid binary installed, when the operator runs `marid instance add work && marid instance start work`, then a server starts on an allocated port with its own directory tree and answers health checks | FR-053, KPI-003 | TEST-INST |
| AC-002 | Given two running instances, when both execute sessions concurrently, then no file outside each instance's tree is written and neither instance's behavior changes | NFR-008, RISK-002 | TEST-INST |
| AC-003 | Given no bearer token, when any API call is made, then the response is 401 and an audit line is written | FR-031/033 | TEST-AUTH |
| AC-004 | Given a `client`-scope token, when it accesses another client's session or an admin route, then 403 with audit | FR-031, scopes | TEST-AUTH |
| AC-005 | Given a token exceeding its rate limit, when it continues calling, then 429 with `retry-after`, and SSE connections above the cap are refused | FR-032 | TEST-AUTH |
| AC-006 | Given a session created via the SDK, when the operator opens the TUI on the same instance, then the session appears and can be continued, and the SDK subscriber receives the TUI-driven updates live | FR-038/042, KPI-001 | TEST-SYNC |
| AC-007 | **SUPERSEDED (2026-07-16) — kept as history, do not test against.** Premise invalid: contract v1.1 (PH-3) corrected the `?after=<lastSeq>` per-session replay claim — the v1 event stream is a live-only firehose and keeps no history to replay. Recovery is authoritative-store re-fetch, and the criterion for it is **AC-008** (Met). ~~Given an SSE client that disconnects mid-run, when it reconnects with `?after=<lastSeq>`, then it receives every missed event for that session exactly once (dedup by aggregate+seq)~~ | FR-036, RISK-006 → superseded by AC-008 | ~~TEST-SYNC~~ (none — see AC-008) |
| AC-008 | Given the server process is killed mid-prompt and restarted, when clients reconnect, then session history is intact and the session is usable per the documented recovery behavior | FR-043 | TEST-SYNC |
| AC-009 | Given two simultaneous prompts to one session, when both are submitted, then behavior matches the documented queue/steer semantics (EXP-001 outcome) with no corruption | FR-040/041 | TEST-SYNC |
| AC-010 | Given a non-allowlisted Telegram user, when they message the bot, then no session is created, nothing is answered, and the attempt is logged | FR-050, INV-001 | TEST-TG |
| AC-011 | Given the allowlisted operator asks the bot a question, when the agent streams its answer, then the Telegram message updates progressively (≥2 s cadence), respects the 4096-char split, and finishes with the complete text | FR-046/048, KPI-002 | TEST-TG |
| AC-012 | Given the channel agent's policy denies a tool, when the model requests it, then a permission prompt appears as an inline keyboard; Deny (or timeout) blocks the tool; Approve allows exactly once | FR-028/052, INV-001 | TEST-TG |
| AC-013 | Given the `marid` profile build, when the binary is inspected/run, then excluded surfaces (desktop, console, cloud, slack, docs-site) are absent and a grep-based hygiene test passes | ADR-0002, FR-060 | TEST-BUILD |
| AC-014 | Given a tagged release, when CI completes, then signed binaries+checksums exist as a **public** GitHub Release (DEC-010) and install/update works via the documented **anonymous** download→verify path on all 3 OSes | FR-060/064, KPI-006 | TEST-BUILD |
| AC-015 | Given an upstream sync PR, when CI runs, then contract tests, migration review, dependency diff, and the delta report are all present; and one real sync cycle has been merged | FR-061, KPI-004 | TEST-SYNCUP |
| AC-016 | Given any log, error output, session export, or channel message, when a configured secret value would appear, then it is redacted. **CLOSED 2026-07-16 as Accepted-with-deviation ([ADR-0019](../adrs/adr-0019-channel-secret-containment-final.md), operator gate) — NOT Met, and the redactor is dropped from scope: do not test against the redaction clause.** Delivered and holding: (a) the audit/config never store the secret, (b) the Telegram bot-token is masked in gateway logs, and (c) the **B2/B4 authorization-boundary containment** that supersedes redaction as the control. **Not delivered and never will be:** the general configured-secret-value redactor across channel egress, general logs/errors, and raw `marid export` — accepted for a single-operator private deployment; **revisit ADR-0019 first if that assumption changes.** | FR-055/059, RISK-007 (accepted, closed) | ~~TEST-SEC~~ (redaction clause untested by design) |
| AC-017 | Given the remediated Telegram gateway (PH-6, full experience), when the operator interacts, then Markdown renders correctly, **files land both ways** (inbound media into the workspace, outbound files/documents sent), **whitelisted slash commands** run, **inline keyboards** work, multi-part replies are separated, and an injected SSE drop auto-recovers (reconnect + re-fetch) — while a non-allowlisted user is ignored and the channel token cannot change its agent or widen tools (INV-001) | FR-036/043/046/048/049, INV-001, ADR-0009 | TEST-TG |
| AC-019 | Given a session the operator **attaches** to a Telegram chat, when a turn is driven from Web/TUI, then it mirrors live into the chat (and a Telegram turn mirrors to Web); a permission surfaces across bound surfaces (first-responder-wins, no double-approve); a channel can **view** a bound session but can only **approve** for sessions it **owns** (view-via-binding, act-via-ownership); an **unattached** session does NOT appear on the channel | FR-066, FR-038/042, INV-001, ADR-0012 | TEST-SYNC |
| AC-020 | Given the GramJS userbot on the Telegram **test DC**, when it sends text / a file / a slash command / taps an inline keyboard, then the bot round-trips correctly over the real protocol (automated, local + on-demand CI) | FR-046/048/049, ADR-0013 | TEST-TG-E2E |
| AC-021 | Given Telegram **Web** driven by Playwright on the test DC, when the bot replies, then Markdown + media **render** correctly in the real client (local + on-demand) | FR-048, ADR-0013 | TEST-TG-UI |
| AC-022 | Given a WhatsApp permission prompt (PH-7), when the operator replies `APPROVE <token>`, then the tool is authorized exactly once; ambiguous / wrong-token / wrong-JID / expired replies are rejected; and the server re-checks scope (a WhatsApp surface cannot approve for a session it does not own — act-via-ownership) | FR-028/052, INV-001, ADR-0015 | TEST-WA |
| AC-023 | Given the fake-WA integration harness (PH-7), when the adapter is exercised, then round-trip + media + streaming-sim shape + attach/mirror + token-permission parse all pass **deterministically with no real account** (the blocking PR gate) | FR-047, NFR-011, ADR-0014 | TEST-WA |
| AC-024 | Given the Marid Gateway (PH-6), when a plain **unattached** TUI/Web/API request is served, its path behaves identically to today; and when the mirroring/registry module faults, it degrades to non-mirrored behavior **without breaking auth or plain clients**; and the gateway's new attach/binding endpoints are **OpenAPI-documented + health-covered + TEST-CONTRACT-pinned** (blast-radius isolation) | FR-034/035, ADR-0011, RISK-024 | TEST-AUTH, TEST-CONTRACT |
| AC-018 | Given the WhatsApp adapter (PH-7), when an allowlisted operator messages it, then a session round-trips over the `channel:` token (INV-001 held: stranger ignored, bound agent, no tool/permission widening), the connection is outbound-only with no public inbound endpoint (OQ-004), and media + throttled streaming behave within WhatsApp limits | FR-047/048/049/050, INV-001, OQ-004, ADR-0010 | TEST-WA |
| AC-025 | Given a machine with a co-installed OpenCode, when the built `marid` binary runs a fresh session, then its **effective** data / state / config read paths are the marid dirs only (`~/.local/share/marid`, `~/.local/state/marid`, `~/.config/marid` — asserted from the boot log / config `directories()`, never dir existence), no foreign auth (`nvidia-custom`) or `model.json` bleeds in, and the co-installed OpenCode's dirs are untouched | NFR-008, DEC-022, ADR-0018, issue-6 | TEST-ISO |
| AC-026 | Given the operator has data-layer `OPENCODE_*` overrides set globally (`OPENCODE_CONFIG_DIR`, `OPENCODE_DB=<abs>`, `OPENCODE_CONFIG`/`OPENCODE_CONFIG_CONTENT`, `OPENCODE_AUTH_CONTENT`), when the binary boots, then it logs a WARN naming each active data-layer override and behaves as disclosed (isolation pierced, not silently defeated); and a plain `OPENCODE_*`-reading third-party plugin still works | DEC-022, INV-002, ADR-0018, issue-6 | TEST-ISO |
| AC-027 | Given the fresh `marid` binary, when it starts, then no upstream "vX available… update now?" popup appears (P-3 config default `autoupdate:false` + bootstrap env), and no auto-update overwrites the binary | DEC-009, P-3, ADR-0018, issue-1 | TEST-ISO, TEST-TUI |
| AC-028 | Given the running agent, when asked "What is your role?" and "what can you do / where do I report bugs?", then it answers as **Marid** (never "opencode", with Marid docs/repo URLs); and a CI guard asserts no emitted system prompt matches `/\bopencode\b/i` outside an allowlist (catches sync-added prompts) | DEC-026, ADR-0018, issue-4 | TEST-IDENT |
| AC-029 | Given the TUI, when the operator sees the startup logo, `/exit` goodbye, sidebar footer, and OS notification, then all read **Marid** (flame + two-tone wordmark, `marid -s` hint, "● Marid {version}"), the OpenCode-GO upsell is gone, and the two-tone wordmark passes a render gate incl. a 256-color terminal (crisp-mono fallback if illegible) | DEC (two-tone), P-2, ADR-0018, issue-3 | TEST-TUI |
| AC-030 | Given the web UI (not binary-embedded), when it loads, then favicon / PWA icons / social-share / `Mark`+`Splash` glyphs / notification icon read **Marid** (from the flame), and no `opencode.ai` remote is fetched (release-notes / notification repointed or disabled) | P-2, ADR-0018, issue-5 | TEST-WEB |
| AC-031 | Given a populated **v0.2.0** OpenCode data dir and no marid dir, when the binary first runs, then it copies auth.json + `${data}/marid` gateway tokens + the sessions DB + `model.json` + Telegram pairing into the marid dirs exactly once (marker prevents re-run, logged), so gateway bearer tokens and Telegram pairing survive the upgrade with no auth outage | DEC-025, ADR-0018, issue-2 | TEST-ISO |
